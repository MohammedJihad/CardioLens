# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Plotting / EDA
matplotlib>=3.7
seaborn>=0.12

# Reading the original .xlsx source (the legacy notebook used pd.read_excel)
openpyxl>=3.1

# Explainability (SHAP global; optional — code falls back to permutation if absent)
shap>=0.44

# Demo app + API
streamlit>=1.30
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
# Tests
pytest>=7.4
