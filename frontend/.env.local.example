# Copy to .env.local for local dev, and set the same key in Vercel → Project → Settings → Environment Variables.
# Point this at your deployed API (Render/HF/etc). No trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
